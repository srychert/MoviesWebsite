export const getloadPersons = (state) => {
	return state.flow.get_all_persons;
};

export const getloadMovies = (state) => {
	return state.flow.get_all_movies;
};

export const getloadActors = (state) => {
	return state.flow.get_all_actors;
};
